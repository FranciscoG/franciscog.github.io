
body {
  margin 0
  overflow hidden;
  font-size: 16px;
}
  
main {
  position: relative;
  padding: 10% 0 0 10%;
}

canvas { 
  position:absolute; 
  display block
}

#terminal {
  color: #fff;
  font-family: $terminal_font 
  font-size: 2em; 
}
